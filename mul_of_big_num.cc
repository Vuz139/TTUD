#include <bits/stdc++.h>
using namespace std;

string a, b;

char *sum(char *a, char *b)
{
    int l1 = a.size();
    int l2 = b.size();

    // int maxL = max(l1, l2);
    // char sum[maxL+5];
    // for(int i = l1; i < maxL; i++){

    // }
}

void multiply()
{
    // int l1 = a.size();
    // int l2 = b.size();
}

void solve()
{
    cin >> a >> b;
}